<%= render partial: 'admin_partials/show/header', locals: { section_title:  t("controller."+params[:controller]),
                                                  section_sub_title:  t("action."+params[:action]),
                                                  page_title: t("strings.law.show")  } %>

<%= render "admin_partials/errors/notices" %>


          <strong><%= t 'activerecord.attributes.law.name' %></strong>
          <p class="text-muted">
            <%= @law.title %>
          </p>

          <strong><%= t 'activerecord.attributes.law.url' %></strong>
          <p class="text-muted">
            <%= @law.url %>
          </p>

          <strong><%= t 'activerecord.attributes.law.description' %></strong>
          <p class="text-muted">
            <%= @law.description %>
          </p>
<%= render partial: 'admin_partials/show/footer', locals: {edit_path: edit_law_path(@law), back_path: laws_path}%>
